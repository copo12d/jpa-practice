spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa_mysql-db
    username: myuser
    password: mypassword
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
